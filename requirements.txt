# Full Red Hawk application dependencies — dashboard + live backend pipeline + target bot.
# A single `pip install -r requirements.txt` brings up the ENTIRE app on any host
# (Docker, Cloud Run, a plain VM, Streamlit Cloud). Floors mirror the agent's uv
# environment in gemini-hackathon/pyproject.toml, which remains the source of truth
# for local ADK development.

# --- Dashboard (Streamlit app: dashboard.py) ---
streamlit>=1.58
pandas>=2.0

# --- Backend pipeline + judge + target bot (Gemini / Vertex via ADK + genai) ---
google-adk>=1.32.0
google-genai>=1.9.0
openinference-instrumentation-google-adk>=0.1.11
arize-phoenix>=7.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
flask>=3.1.3
python-dotenv>=1.0.1
requests>=2.31
pyyaml>=6.0
